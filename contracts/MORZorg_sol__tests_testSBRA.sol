func prog( a, n: int; c: int; ): int

	type	T1: int;

	var		i, j: int;

	const	argald, merlino: int = a + 5;
			twain: string = "unsure";

	func sbra( l: real; ): int
		
		var k: int;

		const sbras: string = "sbra, what else?";
			  sta: int = 4;

	begin sbra
		return prog( k, k, k );
	end sbra

begin prog
	return a + j;
end prog
